# Summary

Adopted Shakrs repository hooks and all 12 independent AQC Rust workspaces. Create-only init preserved tracked files; this separate repair aligns existing tool configuration with enabled policies while preserving project-specific Clippy restrictions.

# Decisions made

- Configure Cargo, Clippy, and rustc policies for package-local lint tables because each package is an independent workspace.
- Configure Rustfmt and toolchain Cargo fields under `[package]`, not `[workspace.package]`.
- Merge baseline Clippy and cargo-deny settings into existing files instead of replacing unmanaged restrictions.
- Add the exact generated Shakrs hook fragment and main-hook inclusion snippet.

# Key files for context

- `.githooks/pre-commit`
- `.githooks/pre-commit.d/shakrs`
- `packages/**/shakrs.json`
- `packages/**/clippy.toml`
- `packages/**/deny.toml`
- `packages/**/rustfmt.toml`
- `packages/**/rust-toolchain.toml`

# Next steps

- Re-run AQC Specular verification with a clean implementation scope.
- Complete equivalent Shackles adoption and the cross-repository adoption marker.

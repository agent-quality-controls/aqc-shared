# Text Engine Lock

## Summary

Synchronized the text file engine lock with `aqc-file-engine-core` 0.7 for locked source verification before the coordinated registry release.

## Decisions Made

- Generated the lock with only the file-engine-core source patch used by this workspace.
- Kept TOML-engine-core out of this workspace because Cargo records unused patches and rejects the resulting lock under the repository hook.

## Key Files For Context

- `packages/file-types/text/aqc-text-file-engine/Cargo.toml`
- `packages/file-types/text/aqc-text-file-engine/Cargo.lock`

## Next Steps

- Commit the coordinated TOML engines with their file-engine-core and TOML-engine-core source patches.
- Regenerate registry locks in release order before publishing.

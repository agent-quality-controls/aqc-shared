# Summary

Released the source state for AQC file-engine-core `0.7.0`, adding unified scalar composition, exact item requirements, and finding selectors required by JSON and YAML engines.

# Decisions Made

- Kept all universal requirement, conflict, and finding vocabulary in `aqc-file-engine-core`.
- Added selectors to shared findings so downstream waiver matching remains format-independent.
- Kept `merge/mod.rs` as the pure public facade for shared merge vocabulary.

# Key Files For Context

- `packages/aqc-file-engine-core/src/finding.rs`
- `packages/aqc-file-engine-core/src/merge/scalar_assertion.rs`
- `packages/aqc-file-engine-core/src/merge/items.rs`
- `packages/aqc-file-engine-core/tests/public_contract.rs`

# Next Steps

- Commit format-core and concrete-engine `0.7.0` workspaces in dependency order.

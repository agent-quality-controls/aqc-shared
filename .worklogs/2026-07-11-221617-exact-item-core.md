# Exact Item Core

## Summary

Replaced item-collection closure with explicit exact collections in `aqc-file-engine-core` and prepared the breaking `0.5.0` release.

## Decisions Made

- Kept explicit required assertions separate from exact members to prevent duplicate conflicts and unintended reconciliation.
- Composed required and exact values through `FileItemRequirement::compose_item` only when building resolved exact state.
- Rejected compatibility aliases for `closed` and `closed_by`.

## Key Files For Context

- `packages/aqc-file-engine-core/src/merge/model.rs`
- `packages/aqc-file-engine-core/src/merge/items.rs`
- `/Users/tartakovsky/Projects/agent-quality-controls/shackles/.plans/2026-07-11-213527-create-only-init-and-exact-items.md`

## Next Steps

- Publish `aqc-file-engine-core 0.5.0`.
- Update and verify dependent AQC engine workspaces against the registry release.

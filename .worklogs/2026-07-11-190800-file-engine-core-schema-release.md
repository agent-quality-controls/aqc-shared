# File engine core schema release

## Summary

Prepared `aqc-file-engine-core 0.4.2` for publication with the public `DottedVersion` schema.

## Decisions made

- Used a patch release because runtime and wire behavior are unchanged.

## Key files for context

- `packages/aqc-file-engine-core/Cargo.toml`
- `.worklogs/2026-07-11-190617-public-value-json-schemas.md`

## Next steps

- Publish core, then resolve and publish the rust-toolchain engine.

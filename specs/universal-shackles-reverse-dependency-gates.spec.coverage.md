# Coverage

- Goal: Specular content checks across every AQC package deny file.
- Approach: Specular required and forbidden crate identities.
- Decision: not-applicable to runtime behavior.
- Files: Specular glob covers every package `deny.toml`.

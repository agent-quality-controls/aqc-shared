# AQC Deny TOML Engine Spec Coverage

## 0. Status

Spec targets:

- content: plan status line
- custom: engine contract

Status: exact accepted contract

## 1. Task Input Contract

Spec targets:

- tree: plan/spec/verifier files
- content: no Shackles code inside AQC engine
- dependencies: no Shackles dependency from AQC crates
- content/custom: Clippy disallowed arrays consume TOML-core item helpers

Status: exact accepted contract

## 2. Current Core Inventory

Spec targets:

- content: required file-engine-core helper usage
- content: required TOML-core helper usage
- custom: `toml-core-items`

Status: exact accepted contract

## 3. Pattern Verdict Matrix

Spec targets:

- tree: accepted AQC TOML engine file layout
- content: rejected maps, rejected aliases, rejected public invalid fields
- dependencies: no concrete engine dependencies
- custom: Clippy keeps glob semantics local but removes duplicate array item mechanics

Status: exact accepted contract

## 4. File Schema Target

Spec targets:

- content: all 0.19.4-supported fields in requirement root
- content: `unused-allowed-org` forbidden
- tests: valid `[bans.build]`, invalid `unused-allowed-org`, accepted `version` fields

Status: exact accepted contract

## 5. Requirement Root

Spec targets:

- content: every raw field in `DenyTomlRequirements`
- custom: every raw field mirrored in `ResolvedDenyTomlRequirements`

Status: exact accepted contract

## 6. Value Types

Spec targets:

- content: every public value type
- content: constructors/accessors/parsers
- enum built-ins: closed enum variants
- custom: `engine-contract`

Status: exact accepted contract

## 7. Core Reuse Contract

Spec targets:

- content: `parse_or_report`
- content: `resolve_maybe`, `resolve_list`, `resolve_items`, `ScalarAssertion::<`, `push_conflict`
- content: `push_mismatch`, `attribution`, scalar/list/table helpers
- custom: `engine-contract`

Status: exact accepted contract

## 8. TOML Core Item Helper Contract

Spec targets:

- tree: `aqc-toml-engine-core/src/items.rs`
- content: helper traits/functions
- dependencies: no concrete engine dependencies from TOML core
- custom: `toml-core-items`
- custom: `clippy-array-reuse`

Status: exact accepted contract

## 9. Validation And Init Behavior

Spec targets:

- tests: parse, shape, duplicate, overlap, closure, repair, pass-through cases
- custom: `cargo-tests`

Status: exact accepted contract

## 10. Package Filetree

Spec targets:

- tree: required crate files, release baseline files, split requirement modules, split reconciliation modules, and forbidden legacy paths
- content: Clippy disallowed reconciliation update

Status: exact accepted contract

## 11. Dependency Contract

Spec targets:

- dependencies: deny engine allowed/forbidden dependencies
- dependencies: TOML-core forbidden concrete engine dependencies
- dependencies: Clippy engine remains isolated from deny engine
- custom: `dependency-shape`

Status: exact accepted contract

## 12. Tests And Spec Obligations

Spec targets:

- content: required test names
- custom: `cargo-tests`
- custom: `clippy-array-reuse`

No uncovered items.

Status: exact accepted contract

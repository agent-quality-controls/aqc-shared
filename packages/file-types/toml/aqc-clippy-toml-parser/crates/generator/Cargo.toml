[package]
name = "aqc-clippy-toml-parser-generator"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Code generator that extracts the clippy.toml schema from upstream Clippy source and emits typed Rust into the sibling types crate."
publish = false

[lints]
workspace = true

[[bin]]
name = "generate"
path = "src/main.rs"

[dependencies]
ureq = "2.10"

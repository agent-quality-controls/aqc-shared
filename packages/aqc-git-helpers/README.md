# aqc-git-helpers

Read-only Git helpers for Agent Quality Controls.

This crate reads worktree state using porcelain output and returns structured
data for lock and verification flows.

It does not mutate repositories.
